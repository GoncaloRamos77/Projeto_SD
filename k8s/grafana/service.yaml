apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: grupo4
  labels:
    app: grafana
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    protocol: TCP
  selector:
    app: grafana
